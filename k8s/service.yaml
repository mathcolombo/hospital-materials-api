apiVersion: v1
kind: Service
metadata:
  name: hospital-materials-api
spec:
  type: LoadBalancer
  selector:
    app: hospital-materials-api
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
